'use client';
import Link from 'next/link';import{useEffect,useState}from'react';import { supabase } from '@/lib/supabase-browser';
import { currentPropertyId } from '@/lib/property';
import { money } from '@/lib/store';
export default function Dashboard(){const[r,setR]=useState<any[]>([]),[p,setP]=useState<any[]>([]),[tenants,setTenants]=useState<any[]>([]),[owner,setOwner]=useState(''),[property,setProperty]=useState('Kost Harmoni');useEffect(()=>{(async()=>{try{const pid=await currentPropertyId();const[a,b,c,d]=await Promise.all([supabase.from('properties').select('name,owner_name,manager').eq('id',pid).single(),supabase.from('rooms').select('id,room_code,rent,status').eq('property_id',pid).order('room_code'),supabase.from('invoices').select('id,tenant_id,period,amount,status').eq('property_id',pid).order('period',{ascending:false}),supabase.from('tenants').select('id,name,room_id').eq('property_id',pid).eq('status','active')]);if(a.error||b.error||c.error||d.error)throw a.error||b.error||c.error||d.error;setProperty(a.data?.name||'Kost Harmoni');setOwner(a.data?.owner_name||a.data?.manager||'');setR(b.data||[]);setP(c.data||[]);setTenants(d.data||[])}catch{}})()},[]);const occ=r.filter(x=>x.status==='occupied').length,av=r.filter(x=>x.status==='available').length,inc=p.filter(x=>x.status==='paid').reduce((a,x)=>a+Number(x.amount),0),month=new Date().toISOString().slice(0,7),monthInvoices=p.filter(x=>String(x.period).slice(0,7)===month),unpaid=monthInvoices.filter(x=>x.status!=='paid').reduce((a,x)=>a+Number(x.amount),0);const pct=r.length?Math.round(occ/r.length*100):0;const statusMeta: Record<'occupied'|'available'|'maintenance', { label:string; bg:string; soft:string; text:string; icon:string }>={occupied:{label:'Terisi',bg:'linear-gradient(145deg,#047857,#10b981)',soft:'#ecfdf5',text:'#047857',icon:'🛏️'},available:{label:'Tersedia',bg:'linear-gradient(145deg,#0369a1,#06b6d4)',soft:'#ecfeff',text:'#0369a1',icon:'🔑'},maintenance:{label:'Perawatan',bg:'linear-gradient(145deg,#b45309,#f59e0b)',soft:'#fffbeb',text:'#b45309',icon:'🔧'}} as const;return <div style={{display:'grid',gap:22}}><div className="top"><div><div className="title">Dashboard</div><div className="sub">Selamat datang{owner?', '+owner:''} · Ringkasan operasional {property}</div></div><Link className="btn" href="/penghuni?aksi=tambah">＋ Tambah Penghuni</Link></div><div className="card" style={{background:'linear-gradient(120deg,#0f172a 0%,#312e81 42%,#7c3aed 72%,#db2777 100%)',color:'#fff',border:'none',boxShadow:'0 22px 55px rgba(76,29,149,.28)',overflow:'hidden',position:'relative',minHeight:150}}><div style={{position:'relative',zIndex:2,padding:8}}><div style={{fontSize:12,fontWeight:800,letterSpacing:2,textTransform:'uppercase',opacity:.78}}>KOSTPRO • PROPERTY COMMAND CENTER</div><div style={{fontSize:'clamp(24px,4vw,32px)',fontWeight:850,margin:'10px 0 6px',overflowWrap:'anywhere'}}>{property}</div><div style={{opacity:.82,lineHeight:1.5}}>{owner?'Dikelola oleh '+owner:'Kelola kamar, penghuni, tagihan, dan keuangan dari satu tempat.'}</div><div style={{display:'flex',gap:10,marginTop:18,flexWrap:'wrap'}}><span style={{background:'rgba(255,255,255,.16)',padding:'7px 12px',borderRadius:999,fontSize:12}}>⚡ {pct}% okupansi</span><span style={{background:'rgba(255,255,255,.16)',padding:'7px 12px',borderRadius:999,fontSize:12,overflowWrap:'anywhere'}}>💳 {money(unpaid)} belum dibayar</span></div></div><div style={{position:'absolute',right:-25,top:-55,fontSize:180,opacity:.12,transform:'rotate(12deg)',pointerEvents:'none'}}>🏠</div><div style={{position:'absolute',right:100,bottom:-65,width:180,height:180,borderRadius:'50%',background:'rgba(255,255,255,.08)',pointerEvents:'none'}}/></div><div className="grid"><div className="card" style={{background:'linear-gradient(145deg,#4f46e5,#7c3aed)',color:'#fff',border:'none',boxShadow:'0 12px 28px rgba(79,70,229,.22)'}}><div className="label" style={{color:'rgba(255,255,255,.8)'}}>🏢 Total Kamar</div><div className="metric" style={{color:'#fff'}}>{r.length}</div><div style={{opacity:.75,fontSize:13}}>Seluruh unit properti</div></div><div className="card" style={{background:'linear-gradient(145deg,#059669,#10b981)',color:'#fff',border:'none',boxShadow:'0 12px 28px rgba(5,150,105,.2)'}}><div className="label" style={{color:'rgba(255,255,255,.8)'}}>🛏️ Kamar Terisi</div><div className="metric" style={{color:'#fff'}}>{occ}</div><div style={{opacity:.78,fontSize:13}}>{pct}% tingkat okupansi</div></div><div className="card" style={{background:'linear-gradient(145deg,#0284c7,#06b6d4)',color:'#fff',border:'none',boxShadow:'0 12px 28px rgba(2,132,199,.2)'}}><div className="label" style={{color:'rgba(255,255,255,.8)'}}>🔑 Kamar Tersedia</div><div className="metric" style={{color:'#fff'}}>{av}</div><div style={{opacity:.78,fontSize:13}}>Unit siap disewakan</div></div><div className="card" style={{background:'linear-gradient(145deg,#ea580c,#f59e0b)',color:'#fff',border:'none',boxShadow:'0 12px 28px rgba(234,88,15,.2)'}}><div className="label" style={{color:'rgba(255,255,255,.82)'}}>💎 Pendapatan Masuk</div><div className="metric" style={{fontSize:'clamp(18px,3vw,22px)',color:'#fff',overflowWrap:'anywhere'}}>{money(inc)}</div><div style={{opacity:.78,fontSize:13}}>Total pembayaran lunas</div></div></div><div className="two"><div className="card" style={{border:'1px solid #ddd6fe',boxShadow:'0 10px 30px rgba(15,23,42,.06)'}}><div className="section-title">🏠 Status Kamar</div><div style={{display:'flex',gap:8,flexWrap:'wrap',marginBottom:16}}>{(['occupied','available','maintenance'] as const).map(s=><span key={s} style={{fontSize:12,fontWeight:700,padding:'6px 10px',borderRadius:999,background:statusMeta[s].soft,color:statusMeta[s].text}}>{statusMeta[s].icon} {statusMeta[s].label}: {r.filter(x=>x.status===s).length}</span>)}</div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(135px,1fr))',gap:12}}>{r.map(x=>{const m=statusMeta[x.status];const tenant=tenants.find(t=>t.room_id===x.id);return <Link href="/kamar" key={x.id} style={{textDecoration:'none',color:'#fff',background:m.bg,borderRadius:18,padding:'17px 15px',minHeight:125,display:'flex',flexDirection:'column',justifyContent:'space-between',boxShadow:'0 10px 22px rgba(15,23,42,.12)',transition:'transform .18s ease,box-shadow .18s ease',overflow:'hidden',position:'relative'}}><div style={{position:'absolute',right:-8,top:-12,fontSize:52,opacity:.13}}>{m.icon}</div><div style={{fontSize:20,fontWeight:850,position:'relative'}}>{x.room_code}</div><div style={{position:'relative'}}><div style={{fontWeight:700,fontSize:13,whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{tenant?.name||'Siap digunakan'}</div><div style={{fontSize:11,opacity:.82,marginTop:4}}>{m.label} · {money(Number(x.rent))}</div></div></Link>})}</div>{!r.length&&<div className="sub">Belum ada kamar.</div>}</div><div className="card" style={{border:'1px solid #fbcfe8',boxShadow:'0 10px 30px rgba(15,23,42,.06)'}}><div className="section-title">💰 Tagihan Bulan Ini</div>{monthInvoices.map(x=><div key={x.id} style={{display:'flex',justifyContent:'space-between',gap:16,padding:'13px 0',borderBottom:'1px solid #f3f4f6',minWidth:0}}><div style={{minWidth:0,overflowWrap:'anywhere'}}><b>{tenants.find(t=>t.id===x.tenant_id)?.name||x.tenant_id}</b><div className="sub">{x.period}</div></div><div style={{textAlign:'right',flexShrink:0}}><b>{money(x.amount)}</b><div><span className={'badge '+(x.status==='paid'?'green':'red')}>{x.status==='paid'?'Lunas':x.status==='partial'?'Sebagian':'Belum Bayar'}</span></div></div></div>)}{!p.length&&<div className="sub">Belum ada tagihan.</div>}</div></div></div>}
