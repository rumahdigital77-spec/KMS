import {rooms,payments} from '@/lib/data';
const money=(n:number)=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(n);
export default function Dashboard(){const occupied=rooms.filter(r=>r.status==='occupied').length;const available=rooms.filter(r=>r.status==='available').length;const income=payments.filter(p=>p.status==='paid').reduce((a,p)=>a+p.amount,0);return <><div className="top"><div><div className="title">Dashboard</div><div className="sub">Ringkasan operasional kost bulan September 2026</div></div><button className="btn">+ Tambah Penghuni</button></div><div className="grid"><div className="card"><div className="label">Total Kamar</div><div className="metric">{rooms.length}</div></div><div className="card"><div className="label">Kamar Terisi</div><div className="metric">{occupied}</div><div className="sub">{Math.round(occupied/rooms.length*100)}% okupansi</div></div><div className="card"><div className="label">Kamar Tersedia</div><div className="metric">{available}</div></div><div className="card"><div className="label">Pendapatan Masuk</div><div className="metric" style={{fontSize:22}}>{money(income)}</div></div></div><div className="two"><div className="card"><div className="section-title">Status Kamar</div><table className="table"><thead><tr><th>Kamar</th><th>Penghuni</th><th>Sewa</th><th>Status</th></tr></thead><tbody>{rooms.map(r=><tr key={r.id}><td><b>{r.id}</b></td><td>{r.tenant}</td><td>{money(r.price)}</td><td><span className={'badge '+(r.status==='occupied'?'green':r.status==='available'?'blue':'amber')}>{r.status==='occupied'?'Terisi':r.status==='available'?'Tersedia':'Maintenance'}</span></td></tr>)}</tbody></table></div><div className="card"><div className="section-title">Tagihan Bulan Ini</div>{payments.map(p=><div key={p.tenant} style={{display:'flex',justifyContent:'space-between',padding:'12px 0',borderBottom:'1px solid #eef0f3'}}><div><b style={{fontSize:14}}>{p.tenant}</b><div className="sub">{p.room}</div></div><div style={{textAlign:'right'}}><b>{money(p.amount)}</b><div><span className={'badge '+(p.status==='paid'?'green':'red')}>{p.status==='paid'?'Lunas':'Belum Bayar'}</span></div></div></div>)}</div></div></>}
