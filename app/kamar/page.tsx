import Link from 'next/link';import {rooms} from '@/lib/data';const money=(n:number)=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(n);export default function Kamar(){return <><div className="top"><div><div className="title">Manajemen Kamar</div><div className="sub">Kelola unit, harga sewa dan status kamar</div></div><Link className="btn" href="/kamar?aksi=tambah">+ Tambah Kamar</Link></div><div className="card"><table className="table"><thead><tr><th>Kamar</th><th>Penghuni</th><th>Harga / bulan</th><th>Status</th><th>Aksi</th></tr></thead><tbody>{rooms.map(r=><tr key={r.id}><td><b>{r.id}</b></td><td>{r.tenant}</td><td>{money(r.price)}</td><td><span className={'badge '+(r.status==='occupied'?'green':r.status==='available'?'blue':'amber')}>{r.status}</span></td><td><Link className="btn secondary" href={'/kamar?detail='+encodeURIComponent(r.id)}>Detail</Link></td></tr>)}</tbody></table></div></>}